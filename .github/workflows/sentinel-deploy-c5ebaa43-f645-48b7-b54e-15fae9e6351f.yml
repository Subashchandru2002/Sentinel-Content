name: Deploy Content to testworkspace1 [c5ebaa43-f645-48b7-b54e-15fae9e6351f]
# Note: This workflow will deploy everything in the root directory.
# To deploy content only from a specific path (for example SentinelContent):
#   1. Add the target path to the "paths" property like such
#    paths:
#    - 'SentinelContent/**'
#    - '!.github/workflows/**'
#    - '.github/workflows/sentinel-deploy-c5ebaa43-f645-48b7-b54e-15fae9e6351f.yml'
#   2. Append the path to the directory environment variable below
#       directory: '${{ github.workspace }}/SentinelContent'

on:
  push:
    branches: [ main ]
    paths:
    - '**'
    - '!.github/workflows/**'  # this filter prevents other workflow changes from triggering this workflow
    - '.github/workflows/sentinel-deploy-c5ebaa43-f645-48b7-b54e-15fae9e6351f.yml'

jobs:
  deploy-content:
    runs-on: windows-latest
    env:
      resourceGroupName: 'testresource1-subash'
      workspaceName: 'testworkspace1'
      workspaceId: 'd59a84a8-4ea6-4c18-b5aa-6e5e66150392'
      directory: '${{ github.workspace }}'
      cloudEnv: 'AzureCloud'
      contentTypes: 'Parser'
      branch: 'main'
      sourceControlId: 'c5ebaa43-f645-48b7-b54e-15fae9e6351f'
      rootDirectory: '${{ github.workspace }}'
      githubAuthToken: ${{ secrets.GITHUB_TOKEN }}
      smartDeployment: 'true'
    permissions:
      contents: write
      id-token: write # Require write permission to Fetch an OIDC token.

    steps:
    - name: Login to Azure (Attempt 1)
      continue-on-error: true
      id: login1
      uses: azure/login@v2
      with:
        client-id: ${{ secrets.AZURE_SENTINEL_CLIENTID_c5ebaa43f64548b7b54e15fae9e6351f }}
        tenant-id: ${{ secrets.AZURE_SENTINEL_TENANTID_c5ebaa43f64548b7b54e15fae9e6351f }}
        subscription-id: ${{ secrets.AZURE_SENTINEL_SUBSCRIPTIONID_c5ebaa43f64548b7b54e15fae9e6351f }}
        environment: 'AzureCloud'
        audience: api://AzureADTokenExchange
        enable-AzPSSession: true

    - name: Wait 30 seconds if login attempt 1 failed
      if: ${{ steps.login1.outcome=='failure' }}
      run: powershell Start-Sleep -s 30

    - name: Login to Azure (Attempt 2)
      continue-on-error: true
      id: login2
      uses: azure/login@v2
      if: ${{ steps.login1.outcome=='failure' }}
      with:
        client-id: ${{ secrets.AZURE_SENTINEL_CLIENTID_c5ebaa43f64548b7b54e15fae9e6351f }}
        tenant-id: ${{ secrets.AZURE_SENTINEL_TENANTID_c5ebaa43f64548b7b54e15fae9e6351f }}
        subscription-id: ${{ secrets.AZURE_SENTINEL_SUBSCRIPTIONID_c5ebaa43f64548b7b54e15fae9e6351f }}
        environment: 'AzureCloud'
        audience: api://AzureADTokenExchange
        enable-AzPSSession: true

    - name: Wait 30 seconds if login attempt 2 failed
      if: ${{ steps.login2.outcome=='failure' }}
      run: powershell Start-Sleep -s 30

    - name: Login to Azure (Attempt 3)
      continue-on-error: false
      id: login3
      uses: azure/login@v2
      if: ${{ steps.login2.outcome=='failure'  }}
      with:
        client-id: ${{ secrets.AZURE_SENTINEL_CLIENTID_c5ebaa43f64548b7b54e15fae9e6351f }}
        tenant-id: ${{ secrets.AZURE_SENTINEL_TENANTID_c5ebaa43f64548b7b54e15fae9e6351f }}
        subscription-id: ${{ secrets.AZURE_SENTINEL_SUBSCRIPTIONID_c5ebaa43f64548b7b54e15fae9e6351f }}
        environment: 'AzureCloud'
        audience: api://AzureADTokenExchange
        enable-AzPSSession: true

    - name: Checkout
      uses: actions/checkout@v3

    - name: Deploy Content to Microsoft Sentinel
      uses: azure/powershell@v2
      with:
        azPSVersion: 'latest'
        inlineScript: |
          ${{ github.workspace }}//.github/workflows/azure-sentinel-deploy-c5ebaa43-f645-48b7-b54e-15fae9e6351f.ps1